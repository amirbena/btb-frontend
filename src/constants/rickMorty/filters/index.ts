export enum LocationTypes {
    NAME = "name",
    TYPE = "type",
    DIMENSIONS = "dimensions"
}

export enum CharacterTypes {
    NAME = "name",
    STATUS = "status",
    SPECIES = "species",
    TYPE = "type",
    GENDER = "gender"
}

export enum EpisodesTypes {
    NAME = "name",
    Episode = "Episode"
}